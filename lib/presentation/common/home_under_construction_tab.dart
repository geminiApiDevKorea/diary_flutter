import 'package:flutter/material.dart';

class HomeUnderConstruction extends StatelessWidget {
  const HomeUnderConstruction({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        '🚧 Under Construction 🚧',
        style: TextStyle(fontSize: 20),
      ),
    );
  }
}
